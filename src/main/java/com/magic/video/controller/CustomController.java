package com.magic.video.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class CustomController {
	protected Logger runLog = LoggerFactory.getLogger(this.getClass());
}
